import 'package:flutter/material.dart';

class ChoiceMenScreen extends StatelessWidget {
  const ChoiceMenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ラブマゲドンしたい男性を選んでな'),
      ),
    );
  }
}
